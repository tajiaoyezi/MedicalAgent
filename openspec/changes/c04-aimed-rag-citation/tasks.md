## 0. 数据表迁移（本 phase 建表 owner）

- [ ] 0.1 创建 `conversations` 基表（对齐 §18）：conversation_id 主键、tenant_id、user_id、module（枚举域 `{aimed, kb_qa}`，区分 AIMed 与 c06 知识库问答会话；MUST NOT 新增 translation 取值——医学翻译文书不经本会话基座、其高风险确认以 subject_type=translation_job 直接挂 translation_jobs.job_id、最近任务经 recent_tasks ref_type=translation_job 路由，均不依赖 conversations.module）、source（来源规范值，如「AIMed 学术助手」/「医疗知识库问答」）、mode、title、allow_pubmed/allow_upload/allow_kb/allow_current_doc 数据源约束（allow_kb=医疗知识库维，按 §8.2 仅 general=✓，其余五模式=✗）、uploaded_files（文件列表 JSON）、created_at、updated_at、deleted_at；含 tenant_id/user_id 隔离列与索引 —— 验证：表存在且按 tenant_id/user_id 可隔离查询；module 枚举域恰为 {aimed, kb_qa}、可区分 aimed 与 kb_qa 会话，供 c06 经本表接口写入 kb_qa 会话（design D1；rag-retrieval/aimed-assistant 会话隔离场景；Decision B kb_qa 会话持久化复用；D2 确认 subject 泛化——translation 走 translation_job subject 不入本表）
- [ ] 0.2 创建 `messages` 基表（对齐 §18）：message_id 主键、conversation_id 外键、tenant_id、user_id、role（user/assistant）、content、mode、parent_message_id（重新生成版本链）、created_at、deleted_at —— 验证：用户输入与每个答案版本各落一条、可按 conversation_id 关联（design D1）
- [ ] 0.3 创建 `citations` 基表（对齐 §18）：citation_id 主键、message_id 外键、tenant_id、cite_index（角标序号 [n]）、source_type（pubmed/upload/kb）、定位指针（pubmed_id/doi/source_url、document_id/page/paragraph_index/section、kb_id/chunk_id）、created_at —— 验证：每条引用绑定 message_id+source_type 定位指针，角标与条目一一对应（design D5；citation-tracing 场景）
- [ ] 0.4 创建 `agent_runs` / `agent_steps` / `tool_calls` 基表（对齐 §18）：用于 AIMed/RAG 内部 AI 任务追踪——agent_runs（run_id、tenant_id、user_id、conversation_id、message_id、status、started_at、ended_at）、agent_steps（step_id、run_id、step_name、input/output 摘要、metrics、created_at）、tool_calls（tool_call_id、step_id、tool_name、args、result、created_at）—— 验证：一次 RAG 编排记一条 agent_run、每节点一条 agent_step（design D3/D7）
- [ ] 0.5 创建 `feedbacks` 基表（对齐 §18，泛化为可承载多来源反馈）：feedback_id 主键、tenant_id、user_id、subject_type 枚举（message/translation_job，区分反馈对象来源）、subject_id（承载 message_id 或 translation_jobs.job_id，替代原 message_id 非空外键硬约束）、rating（赞/踩 或翻译质量评分）、reason（可扩展枚举/文本，承载 AIMed 踩原因枚举〔§8.10.5 原文 7 项：不准确/引用错误/没有回答问题/格式不好/内容太少/内容太长/其他〕∪ 翻译质量反馈维度）、comment、created_at —— 验证：subject_type=message 的 AIMed 踩反馈含原因枚举写入、subject_type=translation_job 的翻译质量反馈可关联 translation_jobs.job_id 写入（design D7；aimed-assistant「踩反馈记录原因」场景；c07 §17.6 翻译质量反馈写入侧，c07 仅写入、owner=c04）
- [ ] 0.6 `agent_checkpoints` 表 V1.0 暂不建（§18 注记：仅为 V1.1 长任务断点续跑预留，本期 RAG 编排无断点续跑需求）—— 验证：本期迁移不创建 agent_checkpoints，design Non-Goals 已注记
- [ ] 0.7 以上表的全部新建均由本 phase（c04）拥有；本 phase 仅消费（不建）c01 的 documents/document_versions、c03 的 document_chunks/embeddings/document_parse_jobs/document_visual_parse_results、c06 的 knowledge_bases/kb_documents、c01 的 recent_tasks、c01 的 audit_logs（建表唯一 owner=c01，c04 仅写入）、c09 的 privacy_redaction_events（建表 owner=c09，c04 仅写入）—— 验证：迁移脚本仅建上述 §18 会话/引用/Agent/反馈表，不重复建上游表（含 audit_logs 归 c01、privacy_redaction_events 归 c09，本 phase 均不建）（proposal Impact 受影响数据表口径）

## 1. AIMed 会话与六模式基座

- [ ] 1.1 在 c04 新建的 `conversations` / `messages` 表上落地 AIMed 会话与消息读写：会话级保存 mode、数据源约束（allow_pubmed/allow_upload/allow_kb/allow_current_doc，allow_kb 按 §8.2 仅 general=✓、其余五模式=✗）、已上传文件列表；用户输入与每个答案版本各为一条 message，会话/消息按 tenant_id/user_id 隔离 —— 验证：会话与消息按 tenant_id/user_id 过滤，跨租户/他人会话不可访问（§24.1「保存内容可进入最近任务并恢复上下文」前置；aimed-assistant「会话按租户与用户隔离」场景）
- [ ] 1.2 建立声明式 `mode_policy` 映射（六模式 × allow_pubmed/allow_upload/allow_kb/allow_current_doc/upload_required/clear_files_on_enter），驱动数据源约束，非硬编码分支；allow_kb 按 §8.2 仅 general=✓、其余五模式（deep_reading/trend_analysis/evidence_tracing/review_gen/writing_assist）=✗ —— 验证：逐模式对照 PRD §8.2 表，禁用数据源（含医疗知识库）在检索阶段被排除（aimed-assistant「AIMed 六大模式与数据源约束」「模式禁用的数据源不被检索」场景；rag-retrieval「智能综述生成/学术写作辅助排除医疗知识库」「通用问答可检索医疗知识库」场景）
- [ ] 1.3 实现模式切换接口：深度文献伴读隐藏「数据源：PubMed」标签、其他模式显示；切到科研态势分析/循证证据溯源清空已上传文件、其他模式保留文件；任意切换强制保留输入框 draft —— 验证：覆盖 aimed-assistant「切换到科研态势分析清空文件」「切换到深度文献伴读隐藏 PubMed 标签」「模式间切换保留输入框内容」三场景（§24.1「模式切换规则符合 PRD」）
- [ ] 1.4 配置六模式输入框占位文案（按 §8.4 原文）并随模式切换返回 —— 验证：切到循证证据溯源时占位文案为「请输入一个临床结论或医学问题，我将为您追溯证据。」（aimed-assistant「占位文案随模式变化」场景）
- [ ] 1.5 实现发送按钮状态机：后端按「模式 + 文件上传/解析状态 + 输入有效文本」三元组计算 `can_send` 与置灰原因，前端只渲染；解析中禁止发送、纯空格视为空 —— 验证：覆盖 aimed-assistant「深度文献伴读未上传文件置灰」「存在解析中文件禁止发送」「通用问答有效文本高亮」「纯空格视为空」四场景（§24.1「发送按钮状态符合状态机」）

## 2. 智能模式匹配与问题处理

- [ ] 2.1 实现智能模式匹配（发送时触发）：一级关键词词典（§8.11.2）+ 二级私有化模型意图分类兜底，命中多模式按固定优先级（深度文献伴读 > 循证证据溯源 > 科研态势分析 > 智能综述生成 > 学术写作辅助）取最高 —— 验证：覆盖 aimed-assistant「关键词触发推荐高亮但不自动切换」「多模式命中按优先级取最高」场景
- [ ] 2.2 推荐模式 ≠ 当前模式时仅高亮推荐 Tab + 引导文案（按 §8.11.2 原文：深度文献伴读「仅基于该文献逐段深度解读，避免外部信息干扰」、科研态势分析「获取该领域发文趋势、热点与演化图谱」、循证证据溯源「检索并验证临床结论，获取高级别证据」、智能综述生成「基于文献自动生成结构化综述，支持溯源」、学术写作辅助「获得段落生成、润色优化与引用补充支持」），不自动切换；实际发送仍用用户当前选定模式 —— 验证：推荐结果只改 UI 提示、不改变数据源与实际发送模式，引导文案与 §8.11.2 逐字一致（aimed-assistant 智能模式匹配「MUST NOT 自动强制切换」「推荐模式展示对应引导文案」）
- [ ] 2.3 实现复合任务与无关问题处理：命中 ≥2 模式给分步操作建议并说明文件/对话在兼容模式间自动保留；通用问答非医学/科研问题返回固定拒答文案；专业模式不符合能力时基于当前模式尽力答、不自动切 Skill —— 验证：覆盖 aimed-assistant「复合任务提示分步操作」「通用问答拒答无关问题」「专业模式不切换 Skill」场景

## 3. PubMed 数据服务（在线/离线双路径）

- [ ] 3.1 定义统一 `PubMedSearchProvider` 接口（search / fetch_detail / import_by_id），检索结果归一为内部 `RetrievedSource`（pubmed_id/doi/title/journal/year/url/abstract/source_type），与上传文件/知识库 chunk 同构 —— 验证：两路径返回条目均含 PMID/标题/期刊/年份等可溯源元数据（pubmed-data-service「PubMed 在线检索与离线缓存双路径」场景）
- [ ] 3.2 【真实接入】实现 `OnlinePubMedProvider`：公网可用时真实调用 PubMed E-utilities（esearch/efetch），含连通性自检（探测可达性 + 超时熔断 + 退避）—— 验证（本期可独立验证断言收敛）：provider 连通性自检/超时熔断/退避可被单测，且在未过 c09 公网门禁时 MUST NOT 向公网发送含 PHI 查询；【带 PMID 的端到端真实检索验收随 c09 公网门禁落地（phase9）】——本期默认公网关闭、redaction-gateway（owner=c09）未接入前仅做 provider 级连通性探测，不合规地端到端真实出公网（与 task 3.6、design D8、pubmed-data-service「公网可用真实调用 PubMed」Scenario 的条件式口径一致）（pubmed-data-service「公网可用真实调用 PubMed」场景）
- [ ] 3.3 【离线降级】实现 `OfflinePubMedProvider`：读离线 PubMed 缓存/预置演示数据/管理员上传文献，同接口返回；选择策略为启动探测 + 运行时熔断，公网不可用/在线超时自动无缝降级离线 —— 验证：断网环境下科研态势分析与循证证据溯源仍可检索作答并出引用（pubmed-data-service「公网不可用走离线缓存闭环」「科研态势分析与循证证据溯源支持离线」场景，§24.1「科研态势分析和循证证据溯源支持 PubMed 或离线 PubMed 缓存」）
- [ ] 3.4 实现 PMC/DOI/URL 取数与授权状态标记：按 id/url 取回并归一化为 RetrievedSource，对每个结果返回授权状态标记（authorized/preview_only/rejected），本 phase 不发起任何向 knowledge_bases/kb_documents 的落库写入；导入授权门禁与「临时预览/正式库落库」裁决以 c06 kb-import 为唯一真值，端到端落库隔离验收移交 c06 kb-import tasks 4.1/4.4/4.9；取数与授权状态写 audit_logs —— 验证：c04 内可观测口径——覆盖 pubmed-data-service「DOI 取数命中白名单标记 authorized」「授权不明确标记 preview_only」「URL 取数授权状态由 c06 裁决」场景，断言返回标记正确且无 KB 落库写入
- [ ] 3.5 实现未授权商业数据库禁止默认抓取：默认数据源限于 PubMed/上传文件/医疗知识库，商业库（万方/知网等）需确认来源版权并授权后方可接入 —— 验证：覆盖 pubmed-data-service「未授权商业库不默认抓取」「商业库需授权方可接入」场景
- [ ] 3.6 实现公网 PubMed 调用前脱敏门禁：调用前消费 c09 redaction-gateway 做 PHI/PII 识别与脱敏（本 phase 不自行实现识别脱敏，仅调用门禁接缝）；识别失败/置信度不足/服务不可用/gateway 未接入时禁止调公网 PubMed、降级离线缓存（本期默认公网关闭、离线优先）；脱敏命中/策略留痕由 c09 redaction-gateway 在公网出口单一写入 privacy_redaction_events，本 phase 仅消费门禁判定、不写该表；本能力调用/审计写 audit_logs —— 验证：覆盖 pubmed-data-service「含 PHI 的查询脱敏后再检索」「脱敏不可用降级离线」场景（端到端公网脱敏验收随 c09/phase9 落地）

## 4. RAG 检索管线

- [ ] 4.0 实现消费 c03「索引就绪」事件构建检索索引：订阅 c03 解析流水线在 `indexing_handoff` 发出的「索引就绪」事件，为对应 document_id/version_id 的 document_chunks/embeddings 构建/刷新 BM25 全文索引与向量倒排索引（c03 仅写 chunk+embedding 并发事件、不建检索索引，本能力为该事件唯一检索侧消费方）；按 (document_id, version_id) 幂等、新版本就绪后替换/失效旧索引 —— 验证：覆盖 rag-retrieval「收到索引就绪事件构建检索索引」「新版本索引就绪后旧索引失效」场景，消除 c03 索引就绪事件的孤儿态（对齐 c03 indexing_handoff 边界与 §16.2 检索可用）

- [ ] 4.1 实现 RAG 编排管线骨架：Query Rewrite → 数据源选择 → 权限过滤 → BM25 ∥ 向量检索 → 合并去重 → rerank → source compression → 注入上下文 → 生成带引用答案；整条管线记一次 `agent_run`、每节点一条 `agent_step` —— 验证：完整流程产出携带可溯源标识的上下文、关键结论可附引用角标（rag-retrieval「完整检索流程产出带引用上下文」场景）
- [ ] 4.2 实现 Query Rewrite（私有化模型优先）与数据源选择（按 mode policy 的 allow_pubmed/allow_upload/allow_kb/allow_current_doc 限定 PubMed/离线缓存、上传文件、医疗知识库、当前文档）；allow_kb=✗ 的模式（deep_reading/trend_analysis/evidence_tracing/review_gen/writing_assist）MUST 排除医疗知识库，仅 general 可检索医疗知识库 —— 验证：当前模式仅含 PubMed 时数据源选择排除上传文件与知识库；智能综述生成/学术写作辅助（含上传但 allow_kb=✗）排除医疗知识库；general 可检索医疗知识库（rag-retrieval「数据源选择遵循模式约束」「智能综述生成/学术写作辅助排除医疗知识库」「通用问答可检索医疗知识库」场景）
- [ ] 4.3 【真实接入 + 离线降级】实现 BM25 全文检索与向量检索并行召回，合并去重（去重键 = pubmed_id 或 (document_id,page,paragraph_index)）；rerank 默认走 c03 reranker 能力，公网/私有化 reranker 不可用时降级 RRF 算法排序 —— 验证：BM25 与向量重叠 chunk 合并去重后再 rerank、同源不重复占引用位；reranker 不可用时 RRF 兜底仍可排序（rag-retrieval「BM25 与向量结果合并去重再 rerank」场景）
- [ ] 4.4 实现 source compression（抽取式优先、必要时私有化模型生成式摘要），压缩不得丢失 page/paragraph 定位元数据；为每个注入 chunk 附稳定 `cite_ref`（序号 + 来源指针）—— 验证：压缩后注入上下文仍保留可定位原文片段与定位元数据（rag-retrieval「完整检索流程产出带引用上下文」场景）
- [ ] 4.5 消费 c03/c06 已入库 chunk 元数据（document_id/source_type/source_title/source_url/pubmed_id/doi/journal/year/section/page/paragraph_index/chunk_text/embedding/chunk_acl），用于检索与溯源定位；其中 chunk_acl 即 c03 document_chunks 的 chunk 级 ACL 物理列（owner=c03，默认继承来源文档 ACL，可严于文档级），本 phase 仅消费不建该列；document_acl 维不落 chunk 元数据：对 c04 自有来源（上传文件/当前文档/团队文档）由本 phase 直接按 c01 document_permissions 派生可见 document 集合执行、不依赖 c06，对 kb 来源可由 c06 注入预计算可见集合作为优化路径 —— 验证：上传文件 chunk 含 page/paragraph_index 段落定位字段、PubMed chunk 含 PMID（rag-retrieval「chunk 携带溯源定位字段」「PubMed chunk 携带 PMID」场景）
- [ ] 4.6 实现检索权限与多租户过滤：按 tenant_id/kb_id/user_id/role/document_acl/chunk_acl 六维过滤 chunk（对齐 §11.9），过滤先于 rerank 与上下文注入，越权文档与越权 chunk 在结果与引用中均不出现；document_acl 维对 c04 自有来源（上传文件/当前文档/团队文档）由本 phase 直接按 c01 document_permissions 派生可见 document 集合独立过滤、不依赖 c06，对 kb 来源可由 c06 注入预计算可见集合作为优化路径；chunk_acl 维按 chunk 元数据过滤 —— 验证：覆盖 rag-retrieval「越权 chunk 被 chunk_acl 维过滤」「越权文档被 document_acl 维过滤」「c04 自有来源文档不依赖 c06 的 document_acl 过滤」「跨租户内容隔离」「权限过滤先于上下文注入」场景
- [ ] 4.7 实现生成答案前公网模型脱敏门禁：注入上下文/用户问题含 PHI 且用公网模型时先经 c09 redaction-gateway 脱敏再调用（本 phase 仅调用门禁接缝、不自行实现识别脱敏），识别失败/服务不可用/gateway 未接入时切 c03 私有化模型（本期默认公网关闭、私有化优先）；脱敏命中/策略留痕由 c09 redaction-gateway 在公网出口单一写入 privacy_redaction_events，本 phase 仅消费门禁判定、不写该表；本能力调用/审计写 audit_logs —— 验证：覆盖 rag-retrieval「注入上下文含 PHI 时脱敏后调用」「脱敏失败切私有化模型」场景（端到端公网脱敏验收随 c09/phase9 落地）

## 5. 文件上传与答案生成

- [ ] 5.1 实现 AIMed 文件上传与管理：单次对话最多 10 个文件、单文件 ≤100MB、不支持加密文件，上传后调 c03 自动解析、解析中禁止发送；文件来源四类（§8.6.1）、格式白名单与 OFD 仅转换/预览/解析/问答/翻译不支持在线编辑（§8.6.2）、文件五态与六个展示字段（§8.6.4）、异常提示文案（§8.6.5）；上传入口在持久化入库或送 c03 解析前消费 c09 security-compliance 的上传时 PHI/PII 识别契约（owner=c09，本 phase 仅消费、不自行实现识别脱敏），按策略「识别并提示/脱敏后送模型/阻止上传」处理，命中且策略=阻止上传时拒绝入库（§19.4 上传时识别时点，端到端随 c09 落地）—— 验证：覆盖 aimed-assistant「文件上传与管理」Requirement 的全部场景（批量上传超过 10 个文件 / 单文件超过 100MB 被去除 / 格式不支持 / 加密文件被拒 / 上传后自动解析且解析中禁止发送 / 解析失败提示 / 引用已删除文件 / 文件状态与展示字段 / 上传文件经 c09 上传时 PHI 识别按策略处理），文案与 §8.6.5 一致（§24.1「支持最多 10 个文件上传」「支持 100MB 单文件限制」）
- [ ] 5.1a 实现 AIMed 本地上传文件落库与解析索引主闭环：AIMed 对话内「本地文件」上传经 PHI 门禁放行后 MUST 经 c01 文档中心上传服务落为 documents/document_versions 并获得 document_id，由该上传入口产生 §10.6 upload_success document_event（产生方=c01、消费方=c03，对齐 c01/c02 的 upload_success/save_new_version 契约），c03 消费 upload_success 完成解析→chunk→索引就绪，再由本能力（rag-retrieval 4.0）据索引就绪事件构建检索索引；conversations.uploaded_files JSON 仅作会话级清单缓存、不替代文档持久化，本能力不直接产生 document_events，citation 的 upload 来源以真实 document_id 定位 —— 验证：覆盖 aimed-assistant「本地上传文件落库为 documents 并触发解析索引主闭环」场景，使「上传→解析→带引用回答」主闭环不断裂、上传来源引用有真实 document_id 支撑（§0.3 主验收闭环、§8.6、§8.9、§16.3 chunk 元数据 document_id/page/paragraph_index、§10.6）
- [ ] 5.2 实现答案生成过程展示：依次「正在理解问题/正在检索内容/正在分析证据/正在生成回答」，检索结束展示「找到 N 篇相关资料，M 篇重点参考」「思考 S 秒」—— 验证：覆盖 aimed-assistant「检索完成展示统计」场景
- [ ] 5.3 实现未检索到资料分支：提示「未找到相关文献，建议调整提问关键词。」并不输出无依据诊疗建议 —— 验证：检索零命中时按文案提示、无无依据建议（aimed-assistant「未检索到资料的提示」场景）
- [ ] 5.4 实现答案结构与免责声明：分点/分章节、保留标题层级、关键结论带 `[n]` 角标、末尾结构化参考资料、底部医疗免责声明（§24.7 第一项）、并按 c09 草稿/辅助建议标记契约（owner=c09，本 phase 消费）将生成内容标记为草稿/辅助建议（§24.7 第二项、§19.2），该标记随展示与保存/写回保留；生成后比对答案角标与 citations 集合，缺失/越界角标触发补全或重生成 —— 验证：覆盖 aimed-assistant「关键结论带引用角标」场景（含免责声明 + 草稿/辅助建议标记，§24.1「答案支持引用角标」、§24.7）
- [ ] 5.5 实现从在线文档发起 AIMed 会话：接收 c05 面板侧 Bridge 传入的已组装文档上下文（全文/选区/结构）、创建新会话并将该上下文作为会话上下文（取数由 c05 负责、建会话由 c04 负责）；AIMed 内短文本/选区翻译归 AIMed，上传完整文档要求翻译全文分流至医学翻译模块 —— 验证：覆盖 aimed-assistant「当前文档发起 AIMed 会话」「全文翻译分流至医学翻译模块」场景（§5.2 核心路径）
- [ ] 5.6 实现 AIMed 高风险答案下发前的人工确认前置消费：AIMed 答案下发前 MUST 交由 c05 服务端 `risk_type` 分类器判定，命中高风险（诊疗/用药/医嘱/临床文书/患者个体信息）时以 `(subject_type=message, subject_id=message_id)` 为键进入 c05 ai-writeback-confirmation 高风险确认链路（确认键泛化为 (subject_type, subject_id)：AIMed/kb_qa=message、医学翻译=translation_job 由 c07 挂载，c04 conversations.module 保持 {aimed, kb_qa} 不增 translation 值），按 `confirmed_role∈{doctor,reviewer}` 裁决，普通用户仅能生成草稿/提交审核、MUST NOT 直接下发；`risk_type` 判定与 `writeback_confirmations` 记录归 c05、本能力仅前置消费并写 audit_logs —— 验证：覆盖 aimed-assistant「高风险 AIMed 答案需医生或审核角色确认后下发」「高风险判定与确认记录归 c05、c04 仅前置消费」「conversations.module 枚举不含翻译取值」场景（§19.2、§24.7 第三项）

## 6. 引用溯源

- [ ] 6.1 在 c04 新建的 `citations` 表承载引用，每条绑定 message_id 与按 source_type（pubmed/upload/kb）的定位指针；参考资料按来源类型结构化渲染（PubMed:「[1] PMID, Title, Journal, Year」/ 上传文档:「[2] xxx.pdf，第 3 页，Methods 段」/ 知识库标 kb 文档与 chunk），角标与条目一一对应 —— 验证：覆盖 citation-tracing「关键结论带角标并对应参考资料」「上传文档引用标注页码段落」场景（§20.3 引用可点击率 ≥95%）
- [ ] 6.2 实现点击引用角标定位 API：PubMed 打开文章详情、上传文件打开预览并定位页码/段落（页码误差 ≤1 页）、知识库打开文档并定位 chunk；定位前实时校验 tenant_id/kb_id/user_id/role/document_acl/chunk_acl 权限 —— 验证：覆盖 citation-tracing「点击 PubMed 角标打开文章详情」「点击上传文件角标定位页码段落」「点击知识库角标定位 chunk」「定位前校验权限」场景（§24.1「点击引用可定位来源」，§20.3 定位成功率 ≥90%、页码误差 ≤1 页）
- [ ] 6.3 实现引用异常分支统一裁决：权限不足/外链失效「该引用源暂时不可用」、原文已删除「该引用源已删除」、chunk 定位失败「已打开来源文档，请手动查看相关段落」，且不中断整篇答案展示 —— 验证：覆盖 citation-tracing「权限不足降级提示」「原文已删除提示」「外链失效提示」「chunk 定位失败降级到文档」场景
- [ ] 6.4 实现引用点击与溯源审计：引用点击、来源定位、外部导入授权写 audit_logs/agent_steps，含操作用户、来源标识与定位结果 —— 验证：引用点击产生含用户与来源标识的审计记录（citation-tracing「引用点击写入审计」场景）

## 7. 答案落地与操作栏

- [ ] 7.1 【真实接入】实现答案「生成在线 Word」：将答案（含引用结构 + 医疗免责声明）渲染为 docx 模型 → 调 c01 文档中心服务端创建服务（owner=c01）将 docx 落为 `documents` / `document_versions` 获得 `document_id`、写入「我的文档中心/应用/AIMed 学术助手/保存内容」（权限按当前用户在该应用文档空间的创建能力校验，本能力 MUST NOT 依赖 c02 编辑器内 `createNewDocument(content, templateId)` 服务端新建变体、MUST NOT 自发明服务端落版契约）→ 由 c01 创建入口产生 §10.6 `upload_success` `document_event`（产生方=c01、消费方=c03，与本地上传文件复用同一 `upload_success` 入库事件、对齐 c01「upload_success 唯一产生方=c01」契约），c03 消费 `upload_success` 解析→chunk→索引就绪、本能力据索引就绪事件构建检索索引使生成文档可被 RAG 检索 → 再经 c02 打开链路把该 `document_id` 在 ONLYOFFICE Document Editor 打开并携带「展开医疗 AI 面板」信号；本能力不直接产生 document_events —— 验证：经 c01 创建服务落库后产生 upload_success、c03 据此解析/索引、docx 经 c02 在 ONLYOFFICE 打开（§24.1「可生成在线 Word 并打开 ONLYOFFICE」，§10.6 生成文档经 upload_success 进入解析/索引、生成→入库事件→解析→索引就绪→可检索链路唯一产生方=c01，§20.3 保存回调成功率 ≥99%）
- [ ] 7.2 【离线降级】当 ONLYOFFICE/Bridge 不可用时的降级演示：保存为 PDF/Markdown 走纯文本/渲染导出（不依赖 ONLYOFFICE），保证离线环境答案仍可落地导出 —— 验证：断开 ONLYOFFICE 时 PDF/Markdown 导出仍可完成（§24.1「保存为 Word / PDF / Markdown」对应 P0）
- [ ] 7.3 实现「保存为」：保存范围（当前回答/当前对话/全部对话）× 格式（在线文档/Word/PDF/Markdown）组合，在线文档/Word 走 c01 文档中心服务端创建服务落库（与 7.1 同链路、由 c01 产 upload_success 供 c03 解析索引）、PDF/Markdown 走导出，命名「yyyymmdd_对话名称」、归档至「我的文档中心/应用/AIMed 学术助手/保存内容」—— 验证：覆盖 aimed-assistant「保存为按规则命名与归档」场景
- [ ] 7.4 实现答案操作栏其余项：复制回答（提示「复制成功」）、分享（复制链接/生成图片/下载图片/关闭分享弹窗，§8.10.4）、赞（图标高亮并记录正反馈写 feedbacks，subject_type=message、subject_id=message_id、rating=赞，§8.10.5）、踩（弹反馈原因并写 feedbacks，subject_type=message、subject_id=message_id、rating=踩 含原因枚举）、重新生成（保留旧版本供对比）、删除（二次确认「是否删除该消息？删除后内容无法恢复。」并同步对话内容/最近任务/历史记录）、生成 PDF / 打开到 ONLYOFFICE；「生成 PPT 大纲」本期为禁用/占位入口、不实现生成逻辑（PPT 生成属 §22.2 V1.1）；按 §13.2 补「翻译/保存后翻译」入口并按 §8.12 分流（选区/短文本走 AIMed 学术写作辅助、整篇/全文分流 c07 建 translation_job）—— 验证：覆盖 aimed-assistant「删除消息二次确认并同步」「点赞记录正反馈」「踩反馈记录原因」「分享四子动作」「PPT 大纲占位不生成」「答案栏选区/短文本翻译走 AIMed」「答案栏整篇翻译分流 c07 建任务」场景
- [ ] 7.5 实现 AIMed 保存内容写入最近任务：保存为/生成在线 Word 成功后在 c01 所建 recent_tasks 落一条 source=「AIMed 学术助手」（§6.4 规范枚举值）、ref_type=conversation、ref_id=conversation_id、按 tenant_id/user_id 隔离、按 (ref_type,ref_id) 幂等的记录；最近任务的展示与恢复编排归 c05，本能力仅负责写入条目 —— 验证：覆盖 aimed-assistant「保存内容写入最近任务」场景（§24.1「保存内容可进入最近任务并恢复上下文」写入侧）

## 8. 指标埋点与验收闭环

- [ ] 8.1 埋点模式识别准确率（识别结果 vs 实际发送模式/标注，记 agent_step）与 PubMed RAG Hit@5（top5 是否含命中源，eval 集评测），分在线/离线模式度量 —— 验证：内置验收集（§20.4）跑出模式识别准确率 ≥85%、Hit@5 ≥80%（§20.3）
- [ ] 8.2 埋点引用可点击率/定位成功率/页码误差（定位 API 成功率与误差记 audit/eval）与踩反馈原因（§8.10.5 固定 7 项：不准确/引用错误/没有回答问题/格式不好/内容太少/内容太长/其他，入 feedbacks）—— 验证：内置验收集跑出引用可点击率 ≥95%、定位成功率 ≥90%、页码误差 ≤1 页（§20.3）
- [ ] 8.3 端到端验收闭环演示（在线 + 离线两条路径）：登录 → AIMed → 上传论文/输入问题 → 选模式 → PubMed 或离线缓存检索 → 生成带引用角标答案 → 点击引用定位来源 → 生成在线 Word 落入文档中心 → ONLYOFFICE 打开并展开医疗 AI 面板入口 —— 验证：逐项核对 §24.1 AIMed 全部验收清单（六模式可切换/切换规则/状态机/10 文件/100MB/引用角标/点击定位/生成在线 Word 打开 ONLYOFFICE/离线 PubMed 缓存支持/保存内容进入最近任务）
